name: Docker Build and Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.jvonallen14 }}
          password: ${{ secrets.dckr_pat_9o2pXdOW4Ty7FJcmWn0gMy3J9tw }}
          
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./dockerfile
          push: true
          tags: ${{ secrets.jvonallen14 }}/firstimage:0.14
