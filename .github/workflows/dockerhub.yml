name: build and deploy to docker hub

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.jvonallen14 }}
          password: ${{ secrets.'@Jvon1234' }}
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: C:\Users\jvona\Revature\Jvon-Allen-code\CSharp.md\HelloWorld\dockerfile
          push: true
          tags: ${{ secrets.jvonallen14 }}/firstimage
